# Deploy en Vercel

1. Importa el repo `kelvincarbonell-prog/sporthq` en Vercel.
2. Framework: `Other`.
3. Build command: vacío.
4. Output directory: vacío.
5. Deploy.

Cada push a `main` publica automáticamente la nueva versión de `index.html`.
